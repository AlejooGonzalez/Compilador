//[SinErrores]

class TestComplexExpressions {
    void met() {
        var result = ((5 + 3) * 2 - 1);
        var logico = (true && false) || (!true && true);
        var comparacion = (5 < 10) && (3 >= 2);
    }
}